# name: Deploy Swiift Check AIAdd commentMore actions

# on:
#   push:
#     branches: [ main ]
#   workflow_dispatch:

# env:
#   REGISTRY: swiftcheckacr.azurecr.io
#   IMAGE_NAME: swiftcheck-api

# jobs:
#   build-and-push:
#     runs-on: ubuntu-latest
    
#     steps:
#     - name: Checkout code
#       uses: actions/checkout@v4
    
#     - name: Set up Docker Buildx
#       uses: docker/setup-buildx-action@v3
    
#     - name: Log in to Azure Container Registry
#       uses: azure/docker-login@v1
#       with:
#         login-server: ${{ env.REGISTRY }}
#         username: ${{ secrets.ACR_USERNAME }}
#         password: ${{ secrets.ACR_PASSWORD }}
    
#     - name: Build and push Docker image
#       uses: docker/build-push-action@v5
#       with:
#         context: .
#         file: ./Dockerfile.azure
#         push: true
#         tags: |
#           ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
#           ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
    
#     - name: Notify deployment complete
#       run: |
#         echo "✅ Image pushed to ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}"
#         echo "🚀 Manually update Container Apps to use new image"